/** @type {import('next').NextConfig} */
const nextConfig = {
 
    images: {
        domains: [
            "cdn.imagin.studio",
            "asset.cloudinary.com"
        ]
    }
}

module.exports = nextConfig
